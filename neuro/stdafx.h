#pragma once
#include "targetver.h"
#define WIN32_LEAN_AND_MEAN
#define _USE_MATH_DEFINES

#include <stdlib.h>
#include <cmath>
#include <math.h>

#define MAX(a,b) (a > b ? a : b)
#define MIN(x,y) (x < y ? x : y)
